<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="fr.jlm2017.pap.AdminActivity">

    <TextView
        android:id="@+id/addMilitant"
        android:text="Ajouter un militant"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:ems="10"
        android:layout_marginTop="37dp"
        android:id="@+id/emailNewMilitant"
        android:hint="email"
        android:layout_centerInParent="false"
        android:layout_below="@+id/addMilitant"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Ajouter le militant"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/addUser"
        android:layout_below="@+id/isAdmin"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="36dp"
        android:padding="30dp" />

    <CheckBox
        android:text="Admin ?"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/isAdmin"
        android:layout_marginTop="27dp"
        android:layout_below="@+id/emailNewMilitant"
        android:layout_alignStart="@+id/addUser" />

</RelativeLayout>
